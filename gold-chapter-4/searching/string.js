const sentences = "I can code every day";

// indexOf (index || -1)
// console.log(sentences.indexOf("day"));

// includes (true | false)
console.log(sentences.includes("can"));

// search (index | -1)
console.log(sentences.search(/EVERY/i));
